Once we have a stagehand script we'll need to add an svg of various cursor pointers to show mouse movements

we should use Bezir curves to make the mouse movements smooth

We should change the cursor depending on the element (ex it shoud change to a text input curosr when we're on a text input, and pointer when we're on a button)

It should have tasteful zoom in and zoom out mimicing screen studio

fast forwards when there are no mouse/keyboard events